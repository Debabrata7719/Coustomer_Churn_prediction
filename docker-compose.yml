services:
  app:
    build: .
    container_name: churn-app
    ports:
      - "8000:8000"
      - "8501:8501"
    volumes:
      - ./Models:/app/Models:ro
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
